git config --global user.name "Anibal0620"
git config --global user.email "Anibalvargas105@gmail.com"

git clone https://github.com/Anibal0620/prueba7.git

git add Anibal.txt

git commit -m "Este es mi primer archivo"

git push origin main
git checkout -b anibal

notepad Anibal.txt

notepad Anibal.txt

git add Anibal.txt

git commit -m "Archivo Cambiado"

git checkout anibal

git push origin anibal
git pull

git checkout main

git merge anibal

git push